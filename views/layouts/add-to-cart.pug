if isAuthenticated
  - var token = csrfToken
  form(action="/cart", method="POST")
    button.btn(type="submit") Add Cart
    input(type="hidden", name="productId", value=id)
    input(type="hidden", name="_csrf", value=token)