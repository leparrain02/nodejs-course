extends ../layouts/main-layout.pug 

block styles
  link(rel="stylesheet", href="/css/product.css")

block content
  if products.length > 0
    ul
      for product in products
        li #{product.product.title} (#{product.qty})
  else
    h1 No product in cart